language: php
sudo: false
php:
  - 5.6
  - 7.0
  - nightly
  - hhvm
matrix:
  allow_failures:
    - php: nightly
cache:
  directories:
    - vendor
install:
  - composer install --prefer-dist --dev --no-interaction
  # Get fresh tests
  - rm -rf vendor/maslosoft/sprite-test
  - git clone https://github.com/Maslosoft/SpriteTest.git vendor/maslosoft/sprite-test
before_script:
  - cd $TRAVIS_BUILD_DIR
script: 
  - bash run-travis
